#include <stdio.h>
#include <locale.h>

int main(){
    setlocale(LC_ALL, "");

int v[20], i;
float soma, media;

for (i = 0; i < 20; i++) {
    printf("Insira a %dª idade: ", i+1);
    scanf("%d", &v[i]);
    fflush(stdin);
}
for (i = 0;i < 20; i++) {
    soma += v[i];
}
media = (soma/20);
printf("\nSoma das idades: %.1f\n", soma);
printf("Media das idades: %.1f\n", media);
}