#include <stdio.h>

int main(){

    int i, n;
    char name[10];

    printf("Digite seu Nome: ");
    gets(name);
    
    printf("Digite um número: ");
    scanf("%d", &n);

    for (i = 0; i < n; i++) {
        puts(name);
    }
}